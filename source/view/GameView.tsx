import React from "react";
import { observer } from "mobx-react";
// import { viewModelContainer } from "../viewmodel";

export const GameView = observer(() => {
  // const vm = viewModelContainer.resolve("GameViewModel");

  return <h1>Game</h1>;
});
